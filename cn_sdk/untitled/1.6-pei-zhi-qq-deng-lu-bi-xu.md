# 1.6 配置QQ登录（必须）

在游戏自身工程AndroidManifest.xml &lt;application&gt; tag中声明如下内容：

```text
<!-- 非常重要 -->
<uses-library android:name="org.apache.http.legacy" android:required="false" />

 <!--注意，这里的101823802必须换成应用自己的qq app id -->
 <meta-data
     android:name="RN_QQ_APP_ID"
     android:value="101823802" />

 <activity
     android:name="com.tencent.tauth.AuthActivity"
     android:launchMode="singleTask"
     android:noHistory="true" >
     <intent-filter>
         <action android:name="android.intent.action.VIEW" />
         <category android:name="android.intent.category.DEFAULT" />
         <category android:name="android.intent.category.BROWSABLE" />
         <!--注意，这里的101823802必须换成应用自己的qq app id -->
         <data android:scheme="tencent101823802" />
     </intent-filter>
 </activity>

 <activity
     android:name="com.tencent.connect.common.AssistActivity"
     android:configChanges="orientation|keyboardHidden|screenSize"
     android:screenOrientation="behind"
     android:theme="@android:style/Theme.Translucent.NoTitleBar" />
```

**注意：请将上述两处的101823802替换为游戏自身对应的QQ APP ID**

